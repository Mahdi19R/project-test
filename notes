1-git init // for initialization of git
2-git add . //
3-git commit -m "message"
(git status(optional))

(if any updates:
1-
   a-git add .
   b-git commit -m "add h2 tag"
2-git add . && git commit -m "add h2 tag"
3-git commit -a -m"add h2 tag")
(git branch(optional))
4-git branch "ali"

